/*
 * This file was generated by the Gradle 'init' task, then trimmed to this
 * package's actual footprint (no native Java code, no compiler plugin).
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/current/userguide/multi_project_builds.html
 */

pluginManagement {
    plugins {
        id "io.ballerina.plugin" version "${ballerinaGradlePluginVersion}"
    }

    repositories {
        gradlePluginPortal()
        maven {
            url = 'https://maven.pkg.github.com/ballerina-platform/*'
            credentials {
                username System.getenv("packageUser")
                password System.getenv("packagePAT")
            }
        }
    }
}

rootProject.name = 'a2a'

include ':a2a-ballerina'

project(':a2a-ballerina').projectDir = file('ballerina')
